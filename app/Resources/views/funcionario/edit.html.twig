{% extends 'base.html.twig' %}

{% block body %}
    <h1>Funcionario edit</h1>

    <a href="{{ asset('uploads/curriculums/' ~ funcionario.curriculum.filename) }}">View brochure (PDF)</a>

    {{ form_start(edit_form) }}
        {{ form_widget(edit_form.curriculum, {'required': false}) }}
        {{ form_widget(edit_form) }}

        <input type="submit" value="Edit" />
    {{ form_end(edit_form) }}

    <ul>
        <li>
            <a href="{{ path('funcionarios_index') }}">Back to the list</a>
        </li>
        <li>
            {{ form_start(delete_form) }}
                <input type="submit" value="Delete">
            {{ form_end(delete_form) }}
        </li>
    </ul>
{% endblock %}
